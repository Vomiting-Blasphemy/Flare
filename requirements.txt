PyQt6>=6.6
pywayland>=0.4.17
jeepney>=0.8
PyInstaller>=6.0
# jeepney is a pure-Python D-Bus client with no C extensions and no
# PyGObject dependency, so it bundles cleanly into the PyInstaller
# single-file binary. This is the only D-Bus library FlareReminder uses.
